declare class Translator {
    language: string;
    constructor(language?: string);
    get lang(): string;
    toBraille(text: string): Promise<string>;
    toNormal(text: string): Promise<string>;
}
export default Translator;
